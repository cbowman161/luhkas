{
  "node_id": "scout",
  "modules": [
    "camera_node",
    "pantilt_node",
    "rover_node",
    "light_node",
    "battery_node"
  ],
  "extra_units": [
    "luhkas"
  ],
  "sync": {
    "host": "100.112.87.59",
    "_comment": "Tailscale 100.x IP — stable for the device's lifetime in this tailnet, no DHCP reservation needed. Bypasses mDNS resolution (which on this network goes through Tailscale anyway). Requires Tailscale up on both ends; if tailscaled is down on the node, fall back to LAN IP 10.10.1.178.",
    "_tailscale_host": "luhkas-scout.local",
    "_lan_host": "10.10.1.178"
  }
}
